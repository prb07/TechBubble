<aside class="ad-banner" aria-label="Advertisement">
  <span class="ad-label">Advertisement</span>

  <a class="ad-image" href="/agency" aria-label="Explore TechBubble Agency">
    <img
      src="/images/ad.jpg"
      alt="TechBubble AI Art creative services"
      width="1080"
      height="1080"
      loading="eager"
      decoding="async"
    />
  </a>

  <a class="ad-link" href="/agency">
    TechBubble Studio <span aria-hidden="true">&nearr;</span>
  </a>
</aside>

<style>
  .ad-banner {
    width: min(100%, 270px);
    margin: 0 auto;
  }

  .ad-label {
    min-height: 24px;
    margin: 0 0 13px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--reader-muted, #77758a);
    font-size: 8px;
    font-weight: 750;
    letter-spacing: 0.14em;
    text-align: center;
    text-transform: uppercase;
  }

  .ad-image {
    overflow: hidden;
    display: block;
    border: 1px solid var(--reader-border, rgba(109, 74, 255, 0.16));
    border-radius: 18px;
    background: var(--reader-surface, #fff);
    box-shadow: var(--reader-shadow, 0 18px 48px rgba(67, 47, 145, 0.1));
  }
<!-- rebuild -->
  .ad-image img {
    width: 100%;
    height: auto;
    display: block;
    filter: saturate(0.88);
    transition:
      filter 0.25s ease,
      transform 0.35s ease;
  }

  .ad-image:hover img {
    filter: saturate(1);
    transform: scale(1.012);
  }

  .ad-image:focus-visible,
  .ad-link:focus-visible {
    outline: 3px solid var(--reader-accent, #725cff);
    outline-offset: 3px;
  }

  .ad-link {
    width: max-content;
    max-width: 100%;
    margin: 11px auto 0;
    display: block;
    color: var(--reader-accent, #725cff);
    font-size: 11px;
    font-weight: 800;
    text-align: center;
    text-decoration-thickness: 1px;
    text-underline-offset: 4px;
  }

  @media (max-width: 900px) {
    .ad-banner {
      width: min(100%, 320px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ad-image img {
      transition: none;
    }
  }
</style>

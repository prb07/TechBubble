<aside class="article-top-ad" aria-label="Advertisement" data-top-ad>
  <span class="ad-label">Advertisement</span>

  <a class="ad-creative" href="/agency" aria-label="Visit TechBubble Agency">
    <span class="ad-orbit ad-orbit-one" aria-hidden="true"></span>
    <span class="ad-orbit ad-orbit-two" aria-hidden="true"></span>

    <span class="ad-brand">
      <span class="brand-mark" aria-hidden="true">
        <i></i><i></i><i></i>
      </span>
      <span>
        <strong>TechBubble</strong>
        <small>Creative studio</small>
      </span>
    </span>

    <span class="ad-message">
      <small>Strategy · Design · AI</small>
      <strong>Build what people remember.</strong>
    </span>

    <span class="ad-cta">Work with us <span aria-hidden="true">↗</span></span>
  </a>
</aside>

<style>
  .article-top-ad {
    width: min(1200px, 100%);
    margin: 22px auto 34px;
  }

  .ad-label {
    margin: 0 0 7px;
    display: block;
    color: var(--reader-muted, #686176);
    font-size: 9px;
    font-weight: 750;
    letter-spacing: 0.16em;
    text-align: center;
    text-transform: uppercase;
  }

  .ad-creative {
    position: relative;
    height: clamp(102px, 9.5vw, 118px);
    padding: 18px clamp(22px, 4vw, 52px);
    overflow: hidden;
    display: grid;
    grid-template-columns: minmax(180px, 0.75fr) minmax(310px, 1.5fr) auto;
    align-items: center;
    gap: 28px;
    border: 1px solid rgba(202, 191, 255, 0.22);
    border-radius: 18px;
    background:
      linear-gradient(90deg, rgba(255, 255, 255, 0.07) 1px, transparent 1px),
      linear-gradient(rgba(255, 255, 255, 0.06) 1px, transparent 1px),
      radial-gradient(circle at 68% 15%, rgba(94, 234, 212, 0.26), transparent 28%),
      linear-gradient(118deg, #17102f 0%, #3f216f 52%, #6d4aff 100%);
    background-size: 24px 24px, 24px 24px, auto, auto;
    box-shadow: 0 14px 34px rgba(39, 24, 88, 0.16);
    color: #fff;
    text-decoration: none;
    isolation: isolate;
  }

  .ad-creative::after {
    position: absolute;
    z-index: -1;
    right: 18%;
    bottom: -74px;
    width: 220px;
    height: 150px;
    border-radius: 50%;
    background: rgba(242, 157, 255, 0.18);
    filter: blur(2px);
    content: "";
    transform: rotate(-14deg);
  }

  .ad-brand,
  .ad-message {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
  }

  .ad-brand {
    gap: 12px;
  }

  .brand-mark {
    position: relative;
    width: 38px;
    height: 38px;
    flex: 0 0 auto;
  }

  .brand-mark i {
    position: absolute;
    width: 12px;
    height: 27px;
    border-radius: 10px;
    background: linear-gradient(180deg, #62f2e6, #c18cff);
    transform: rotate(35deg);
  }

  .brand-mark i:nth-child(1) {
    top: 8px;
    left: 2px;
  }

  .brand-mark i:nth-child(2) {
    top: 0;
    left: 14px;
    background: linear-gradient(180deg, #ffb6e4, #8d74ff);
  }

  .brand-mark i:nth-child(3) {
    top: 10px;
    left: 26px;
    height: 22px;
  }

  .ad-brand strong,
  .ad-brand small,
  .ad-message strong,
  .ad-message small {
    display: block;
  }

  .ad-brand strong {
    font-size: 18px;
    letter-spacing: -0.03em;
  }

  .ad-brand small,
  .ad-message small {
    margin-top: 2px;
    color: rgba(255, 255, 255, 0.7);
    font-size: 9px;
    font-weight: 750;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .ad-message {
    min-width: 0;
    flex-direction: column;
    align-items: flex-start;
  }

  .ad-message strong {
    margin-top: 4px;
    font-size: clamp(20px, 2.2vw, 31px);
    line-height: 1;
    letter-spacing: -0.04em;
    white-space: nowrap;
  }

  .ad-cta {
    position: relative;
    z-index: 1;
    min-height: 42px;
    padding: 0 17px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 7px;
    border: 1px solid rgba(255, 255, 255, 0.38);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.13);
    font-size: 12px;
    font-weight: 800;
    white-space: nowrap;
    backdrop-filter: blur(8px);
  }

  .ad-orbit {
    position: absolute;
    z-index: -1;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 50%;
  }

  .ad-orbit-one {
    top: -92px;
    right: 7%;
    width: 210px;
    height: 210px;
  }

  .ad-orbit-two {
    top: -58px;
    right: 11%;
    width: 140px;
    height: 140px;
  }

  .ad-creative:hover .ad-cta {
    background: rgba(255, 255, 255, 0.22);
  }

  .ad-creative:focus-visible {
    outline: 3px solid var(--reader-accent, #6d4aff);
    outline-offset: 3px;
  }

  @media (max-width: 760px) {
    .article-top-ad {
      margin: 16px auto 26px;
    }

    .ad-creative {
      height: 126px;
      padding: 17px 19px;
      grid-template-columns: minmax(0, 1fr) auto;
      grid-template-rows: auto auto;
      gap: 8px 14px;
      border-radius: 15px;
    }

    .ad-brand {
      align-self: end;
    }

    .ad-message {
      align-self: start;
      grid-column: 1 / -1;
      grid-row: 2;
    }

    .ad-message small {
      display: none;
    }

    .ad-message strong {
      max-width: 18ch;
      margin-top: 0;
      font-size: clamp(19px, 6vw, 26px);
      line-height: 1.06;
      white-space: normal;
    }

    .ad-cta {
      min-height: 38px;
      padding: 0 13px;
      grid-column: 2;
      grid-row: 1;
      font-size: 11px;
    }
  }

  @media (max-width: 420px) {
    .ad-brand small {
      display: none;
    }

    .brand-mark {
      width: 33px;
      transform: scale(0.88);
      transform-origin: left center;
    }

    .ad-cta {
      padding: 0 11px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ad-creative,
    .ad-cta {
      transition: none;
    }
  }

  @media print {
    .article-top-ad {
      display: none;
    }
  }
</style>
